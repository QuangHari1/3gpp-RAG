# Copy to .env and add a project API key. Never commit the populated .env file.
OPENAI_API_KEY=
# Required only when [llm].provider = "deepseek". Keep OPENAI_API_KEY for OpenAI embeddings.
DEEPSEEK_API_KEY=
